# 居家陪伴机器人 需求文档（后端 A / B 部分）

版本：V0.2（草稿，待团队评审。V0.1→V0.2：新增 A8–A11 挂机态需求、§6 提案 5 时间基准、§11 待确认 7/8，均标【新增】） 日期：2026-09-23
责任范围：本文档覆盖**模块 A（视觉感知）**与**模块 B（业务中枢）**；模块 C（前端表情窗口）由前端组负责，本文仅描述对其的依赖。
接口规范：以 `api_doc.md` V1.0 为唯一通信标准，本文不重复定义字段，只引用。

---

## 1. 项目背景与目标

面向居家老人（尤其是独居老人）的桌面陪伴机器人。摄像头持续感知老人面部状态，机器人在合适时机主动问候、聊天、安抚，并在困倦、跌倒、生命体征异常等场景下语音预警。

产品原则（按优先级）：

1. **安全第一**：疑似跌倒、长时间闭眼等安全事件优先级最高，压倒一切静默规则。
2. **低打扰**：老人专注做事/看电视时自动静默，不烦人。
3. **稳定不崩**：任何传感器异常、断连、断网都不得导致程序崩溃，一律降级兜底。
4. **本地优先**：视频画面只在本地处理，不上传任何图像数据。

## 2. 系统总览与分工

```
[摄像头] → A 视觉感知 → (TCP 8000, JSON) → B 业务中枢 → (TCP 8001, 状态串) → C 表情窗口
                                          └→ ASR / LLM / TTS（语音交互闭环）
```

| 模块 | 职责 | 负责方 |
|---|---|---|
| A 视觉感知 | 摄像头采集、人脸 468 关键点、EAR/眨眼、头姿、表情、视线、rPPG 指标计算，按帧发 JSON | **后端组** |
| B 业务中枢 | 指标接收与防抖、状态融合判决、触发规则、ASR、对话引擎、TTS、状态下发 | **后端组** |
| C 表情窗口 | 接收状态字符串，切换卡通表情图 | 前端组 |

## 3. 功能需求 —— 模块 A（视觉感知）

### A1. 摄像头采集与人脸关键点
- OpenCV 读取摄像头（≥15fps，640×480 起步），MediaPipe FaceMesh 提取 468 关键点。
- FaceMesh 固定参数：`refine_landmarks=True`（保留虹膜 10 点，供视线估算）；`max_num_faces` 默认 3（config 可调），多人脸时按包围盒面积锁定主脸（§12.2 P2）。
- 摄像头编号默认 `0`；打不开时回退 `1`，仍失败则报错退出并在日志提示（不静默吞掉）。

### A2. EAR 眼睑开合度与眨眼检测
- 分别计算左右眼开合比例 `eye_left_ratio`、`eye_right_ratio`（上下眼睑关键点距离 / 水平距离）。
- 协议字段 `ear` = 左右均值，保留 **2 位小数**；左右分值仅内部用于打标签，不新增传输字段（遵守 api_doc 字段冻结）。
- 累计眨眼次数 `blink_cnt`（程序启动起累计，int）；眨眼频率由 B 按窗口换算，A 不发频率字段。
- 判定闭眼：EAR 低于阈值持续超时（默认阈值写入配置文件）。

### A3. 头部姿态 pitch / yaw / roll
- 由关键点几何比例估算 `pitch`（低头/抬头）、`yaw`（左偏/右偏），单位度、保留 2 位小数。
- `roll` 为协议必填字段：由左右眼角连线倾角计算，同样 2 位小数。
- 检测低头、歪头；**头部持续倾斜/骤变是「疑似跌倒」的原始信号**（判决在 B，A 只报角度）。

### A4. 单帧标签判定（if-else，不训练模型）
- 每帧直接打标签，输出**四选一**，小写英文：`normal / tired / sad / blank`，**禁止中文或大小写变体**。
- 参考规则（阈值集中配置，联调期调整）：

| 优先级 | 标签 | 单帧判定（满足即选） |
|---|---|---|
| 1 | `tired` | `ear` 低于疲劳阈值（默认 0.18）或眨眼间隔异常 |
| 2 | `sad` | 嘴角弧度 + 眉眼几何低于平静阈值 |
| 3 | `blank` | 双眼睁开但视线长时间无变化（虹膜点位移低于阈值） |
| 4 | `normal` | 以上皆否 |

- 单帧标签准确率目标 70%+；**最终状态判定在 B**，A 不做防抖。
- **任务书中的"烦闷"不设 A 侧标签**（2026-09-23 裁定，issue #4）：烦闷是跨时间积累的复合情绪，
  单帧 if-else 无法可靠判定；由 **B 在滑动窗口融合时推断**（结合眨眼频率异常、头姿频繁晃动、
  标签时序抖动等信号）。A 若日后续加 `annoyed` 标签属协议变更，须走全员确认。

### A5. 视线估算（服务于 blank 标签）
- 由虹膜关键点估算视线方向与位移量；长时间无视线移动 + 无眨眼波动 → 支持 A4 的 `blank` 判定。
- `absent` 状态判决在 B（滑动窗口防抖后），A 只出原始特征。

### A6. rPPG 心率/呼吸率估算（二期，可作加分项）
- 普通摄像头非接触估算 HR/RR（面部 ROI 平均色值信号 + 峰值检测）。
- 输出 `hr`、`rr` 两个数值字段。**仅作日常参考，界面/语音必须注明"不能替代医疗设备"**。
- 精度受光照、运动影响大，定位为参考级指标；受挫时先保其余功能。

### A7. 数据出口（双模式，先 CSV 后 Socket）
- **CSV（先做，B 开工的前置条件）**：每帧一行，表头与列序固定为
  `timestamp,has_face,ear,blink_cnt,pitch,yaw,roll,emo_feature`，与 api_doc §3.2 字段一一对应；UTF-8 无 BOM，`\n` 换行；数值 2 位小数，布尔小写 `true/false`，标签小写英文。
- **Socket（后做）**：TCP 服务端 `127.0.0.1:8000`，每帧一行 JSON + `\n`，字段与 CSV 完全一致。
- CSV 与 Socket 输出必须来自同一套指标计算代码，保证联调时行为一致。
- **无人脸帧的处理**：Socket 照发心跳 JSON（`has_face=false` + 其余字段默认值，维持 api_doc §3.3——B 靠心跳区分"没人"与"A 挂了"）；CSV **跳过**无人脸行（离线调试文件保持干净）。⚠️ 与组内"无人脸直接跳帧不发包"的提议有出入，按此执行需评审确认，见 §6 提案 4。

### A8. 基线标定持久化【新增】（P1，已实现 2026-09-23）
- 【依据：场景推导】独居老人无法配合每次开机"保持正常坐姿 2 秒"；陪伴机器人每日自启、断电重启是常态，"演示态标定"在挂机态不成立。
- 首次标定成功后将基线（pitch/yaw/roll/嘴角弧度均值）写入 `backend_A/baseline.json`；此后启动直接加载，跳过 25 帧标定流程。
- `c` 键手动重标定保留，重标定结果覆盖保存；加载的旧基线若与当前姿态持续失配（连续 90 帧极端偏移），自动重采——**仅当次会话生效**，持久化仍需按 `c` 人工确认，防止跌倒等异常姿态被固化为新基线。
- 隐私边界：基线属个人生物特征**派生数据**，仅存本地不上传（见 §7）。
- 验收：保留基线文件重启 → 不重标定直接输出相对角度；删除 baseline.json → 回退首次标定流程。

### A9. 启动自检与降级声明【新增】（P1，已实现 2026-09-23）
- 【依据：§7 稳定性/日志要求落地化；联调排查痛点】启动时依序自检：摄像头可读 → FaceMesh 模型加载 → data 目录可写 → Socket 端口可绑 → 环境照度采样，末行输出"自检通过 / 降级项：X"。
- 任一项失败不崩溃（§1 原则 3）：端口被占 → 降级纯 CSV + 警告；摄像头缺失/磁盘不可写为致命项，明确原因退出。
- 退出码约定：`0` 正常（含降级），非 `0` 致命失败——供守护脚本或 B 判断 A 健康状态。
- 验收：拔摄像头启动 → 原因明确 + 退出码非 0；端口占用启动 → 降级 CSV 模式 + 退出码 0。

### A10. 照度自检【新增】（已实现 2026-09-23，见 vision_a.py 与 README）
- 【依据：场景推导】夜间起夜不开灯、白天拉窗帘看电视 → FaceMesh 检不出人脸 → B 会把"太暗"误判为"无人"，而二者处置完全不同（静默 vs 提示开灯/查设备）。
- 每帧计算画面亮度均值，低于 `low_light_th`（默认 45，0–255）时：状态切换时控制台告警一次（不刷屏）、预览窗红字 LOW LIGHT、退出摘要统计低照度帧数。
- 不新增协议字段；B 可结合该告警与 has_face 流区分"黑屋 / 离开 / 掉线"三种异常（issue #2、#5 语义的补充）。

### A11. 数据保留与清理【新增】（P2，已实现 2026-09-23）
- 【依据：《个保法》最小必要原则；长期挂机磁盘管理】CSV 含生理与行为指标（EAR、头姿、标签、HR/RR），属敏感个人信息，只存本地且应限期保留。
- A 启动时自动清理 `data/` 下超过保留期（默认 7 天，配置化 `csv_retention_days`）的采集文件；交付 B 的样例 CSV 已入 git，本地被清理可随时恢复。
- 验收：放入过期命名的 CSV 启动后被清理，保留期内文件不受影响。

## 4. 功能需求 —— 模块 B（业务中枢）

### B1. 指标接收与滑动窗口防抖
- TCP 客户端连接 A（8000），解析 JSON 流；断连自动重连（指数退避）。
- 滑动窗口防抖（默认 5 秒窗口，参数可配）：窗口内多数投票/加权判决，消除单帧毛刺。
- **A 单帧标签 → B 融合状态映射**：`normal→NORMAL`、`tired→TIRED`、`sad→SAD`、`blank→ABSENT`。命名差异（A 报 `blank`，B 对外叫 `absent`）是约定，联调时勿混用。

### B2. 状态融合判决
输出状态机（优先级从高到低）：

| 优先级 | 状态 | 判决依据（窗口内） |
|---|---|---|
| 1 | `warning`（疑似跌倒/预警） | 头部持续倾斜或角度骤变 + 长时间闭眼 |
| 2 | `tired` | EAR 低于阈值 + 眨眼频率异常，持续超过阈值时长 |
| 3 | `sad` | 表情 `sad/low` 持续 |
| 4 | `absent` | 眼神呆滞/长时间无视线变化 |
| 5 | `normal` | 以上皆否 |
| — | `focus`（专注，B 内部使用，不下发 C） | 视线稳定 + 有规律眨眼 → 进入静默模式 |

- 状态切换需在窗口内连续满足 N 帧才切换（防抖），`warning` 例外：命中即触发。
- 高优先级状态可打断低优先级的进行中对话。

### B3. 触发规则引擎（可配置，集中在一个 rules 配置文件）

| 规则 | 触发条件 | 动作 |
|---|---|---|
| 定时问候 | 到达早晨/下午/晚间时段且用户空闲 | 主动语音问候，C 切问候表情 |
| 情绪关怀 | `sad` 持续超阈值 | 发起关心对话（LLM 安抚话术） |
| 休息提醒 | `tired` 持续超阈值 | 语音提醒休息，C 切疲惫表情 |
| 失神关心 | `absent` 持续超阈值 | 轻声问候拉回注意力 |
| 安全预警 | `warning` | **立即**语音预警；持续无响应则升级（见 B7） |
| 体征异常 | HR/RR 超出正常范围持续 N 分钟 | 语音建议休息（弱提醒，注明仅供参考） |
| 低打扰静默 | `focus` 状态 | 压制问候/关心类主动交互，仅保留安全预警 |
| 频率上限 | — | 主动交互每小时不超过 N 次（默认 3，可配），防止烦人 |

### B4. 语音 ASR（听）
- 麦克风采集（pyaudio），ASR 转文字。
- 推荐路径：本地 faster-whisper（CPU 可跑、离线、无隐私顾虑）；备选云 API（讯飞/百度）。
- 仅在机器人说话后/用户主动唤醒后开启监听窗口，平时不录音。

### B5. 对话引擎（想）
- 调用大模型 API（推荐 DeepSeek/GLM 等低价国产 API）生成回复，prompt 注入当前用户状态与角色设定（温和、简短、口语化的陪伴语气）。
- **断网/超时降级**：本地预置安抚话术库随机应答，保证对话不中断。
- 本地轻量 LLM（Qwen 0.5B~1.5B）作为完全离线的备选方案，性能不够则不做。

### B6. 语音 TTS（说）
- 文字转语音并播放。推荐 pyttsx3（零依赖离线起步），效果不满意换 edge-tts（免费、更自然）。
- 播放期间暂停 ASR 监听，避免自听自答。

### B7. 状态下发与升级链路
- TCP 服务端 `127.0.0.1:8001`，按 api_doc §4 发状态字符串 + `\n`。
- C 断连时 B 照常运行（语音链路不依赖 C）。
- 安全预警升级：`warning` 持续无响应 → 语音加大提醒（二期可扩展：通知家属，见 §6 提案 3）。

### B8. 离线开发模式
- `offline_mode=True`：读 A 的 CSV 回放指标流，完整跑通 B1~B6，不依赖摄像头与 A。
- `offline_mode=False`：切换 Socket 实时对接。**两种模式共用全部业务代码，仅数据源不同。**

> 【补充建议，供 B 组参考，不构成承诺】"在场/离场"时长可从现有心跳流直接推导（有脸帧→无脸心跳的时序），是**作息规律分析**（起床/午睡/夜起/该起未起）的原材料；作息异常是独居老人预警的高价值信号（ElliQ 等陪伴产品标配 daily routine insights）。无需 A 新增字段，但依赖 §6 提案 5 的绝对时间基准才能跨天对齐。

## 5. 对模块 C 的依赖（前端组实现，此处仅声明）

- C 为 Socket 客户端，连 B 的 8001，收到状态字符串即切换对应表情图。
- C 侧表情集：😊正常、😪疲惫、😔难过、😶发呆、👋问候、⚠️预警 —— **与现有协议 4 状态存在缺口，见 §6 提案 2**。
- C 收到未知内容/断连 → 兜底显示 normal（api_doc §4.3）。

## 6. 接口变更提案（⚠️ 需全员确认后更新 api_doc.md，禁止私自改动）

现有 `api_doc.md` V1.0 与本次需求存在三处缺口：

1. **A→B JSON 缺 rPPG 字段**：需求 8（心率/呼吸率）要求新增 `hr`（float）、`rr`（float）。若 rPPG 定二期，字段可先在 CSV 中预留，协议暂不动。
2. **B→C 状态集不够用**：协议只有 `normal/sad/tired/absent` 四种，但 C 需要「问候」「预警」共 6 种表情。提案：增加 `greeting`、`warning` 两个状态串。兜底规则不变（未知 → normal）。
3. **通知家属渠道未定义**：交互效果提到"必要时通知家属"，涉及外部通信（微信/短信/电话），超出当前三模块架构。建议列二期，暂以"语音加大提醒"降级。
4. **A 单帧标签枚举细化**：V1.0 中 `emo_feature` 描述为"normal/low/tired"，现按 A 组实现细化为 `normal/tired/sad/blank` 四值枚举；同时明确无人脸帧"Socket 心跳照发、CSV 跳行"的处理，及 CSV 固定表头。`api_doc.md` 已同步升为 **V1.1 草案**（变更处标 ⚠️），待全员确认。
5. **timestamp 时间基准变更【新增】**：现协议 `timestamp` 为程序运行时间（每次启动归零）。后果：A 重启后时间轴跳变、B 无法对齐 A 的日志、跨天作息分析无从谈起、B 的语音事件与视觉事件对齐靠猜。提案二选一：① 直接改用 Unix epoch 秒（本地单机无 NTP 回拨风险，B 可换算绝对时间，推荐）；② 维持运行时间，启动首包附带 `wall_clock` 对齐字段。随 §11 待确认 7 拍板。

## 7. 非功能需求

| 类别 | 要求 |
|---|---|
| 实时性 | 视觉处理 ≥15fps；状态端到端延迟（感知→C 切表情）< 3 秒 |
| 稳定性 | 无人脸/断连/断网/摄像头被占用均不崩溃，降级运行；A、B、C 任意一个重启，其余自动重连 |
| 性能 | 全部视觉推理在普通 CPU 笔记本上运行（无 GPU 依赖） |
| 隐私 | 图像数据只进内存，不落盘不上传；日志不得包含画面截图 |
| 可配置 | 所有阈值（EAR、时长、窗口、问候时段、频率上限）集中在配置文件，不写死在代码 |
| 日志 | A/B 分模块记录关键事件（状态切换、触发规则、异常重连），便于联调排查 |

## 8. 开发阶段与里程碑

| 阶段 | 内容 | 交付物 |
|---|---|---|
| 一（A 先行，B 随后） | A：A1~A5 视觉代码 + CSV 导出（**交付含全部 4 种标签的 CSV 样例是 B 开工的前置条件**）；B：拿到 CSV 后 `offline_mode=True`，开发 B1~B3、B5~B7 | A 的 CSV 测试文件；B 的状态判决与对话闭环 |
| 二 | B 补语音（B4、B6）；A 起 Socket 服务（A7 在线模式）；B 切 `offline_mode=False` 联调 | A→B 实时链路，语音闭环 |
| 三 | C 接入，三方联调（启动顺序 A→B→C），按 §10 验收 | 全链路演示 |
| 四（加分/二期） | A6 rPPG、预警升级通知家属、表情模型优化 | 视评审排期 |

## 9. 技术风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| mediapipe 版本不兼容（新版移除 face_mesh 旧接口） | A 阻塞 | 锁定含 `solutions` 的版本（见 requirements.txt 注释），环境搭建为阶段一第一项任务 |
| pyaudio 在 Windows 编译失败 | B 阻塞 | 优先找预编译 wheel；实在不行退化为纯文字对话（TTS 保留） |
| rPPG 精度不足 | 需求 8 打折 | 已定位为二期参考级指标，失败不阻塞主线 |
| LLM API 延迟/费用/断网 | 对话体验 | 预置本地话术库降级；回复限长（≤3 句） |
| 表情几何规则准确率低 | 情绪判决误报 | 滑动窗口防抖 + 阈值可配；必要时二期换轻量训练模型 |
| Django 原型与 B 的关系不明 | 架构混乱 | 见 §11 待确认项 4 |

## 10. 验收标准

- **A**：CSV 表头/列序与 api_doc §3.4 完全一致，样例文件覆盖 `normal/tired/sad/blank` 全部 4 种标签；Socket 无人脸心跳 `has_face=false` 不中断；数值均 2 位小数；拔摄像头不崩溃。
- **A【新增，挂机态】**：保留基线重启不重标定、删除 baseline.json 能重标定；启动自检末行输出"自检通过/降级项"，拔摄像头退出码非 0；低照度时出现告警；过期 CSV 启动时被清理。
- **B**：给定一段 CSV 回放，能输出符合 §5 优先级的正确状态序列；断开 A 后自动重连；断网时对话走本地话术库不中断。
- **联调**：按 A→B→C 顺序启动，全部状态能正确驱动 C 的表情切换；人为遮脸 30 秒不产生误报；专注状态下主动交互被静默。
- **安全场景演示**：模拟头部骤倾 → `warning` 立即触发语音预警（不被低打扰规则压制）。

## 11. 待确认决策清单（团队评审时逐条拍板）

1. A→B 是否现在就加 `hr/rr` 字段，还是随 rPPG 二期再加？（影响 CSV 与协议版本）
2. B→C 扩展 `greeting/warning` 两状态的提案是否通过？（需前端组同步表情映射）
3. ASR/TTS/LLM 选型：本地优先（faster-whisper + pyttsx3 + 云 API）还是全云？（涉及费用与断网策略）
4. Django 原型的去留：B 是纯 Socket 服务（推荐，轻量），还是把 Django 当 B 的骨架/管理后台？（现有 Django 只是个空壳）
5. 「通知家属」二期用什么渠道（微信/短信），还是砍掉？
6. 主动交互频率上限、问候时段的具体数值。
7. 【新增】timestamp 时间基准（§6 提案 5）：直接换 Unix epoch 秒（推荐），还是加 `wall_clock` 对齐字段？（协议变更，需全员确认）
8. 【新增】是否做"夜间时段降载"（如 23:00–7:00 降到 2fps 仅在场检测，或暂停采集只留心跳）：省电+隐私友好，但涉及"摄像头何时算开着"的伦理展示，A 侧可实现，需产品拍板后进配置。

---

## 12. 算法全景与分层（2026-09-22 补充）

> 团队规划的 25 项算法不是 25 个独立模块：绝大多数"指数"是同一组原始信号在 B 侧的加权合成。
> A 的职责边界：**只采信号、只出特征，不做指数合成，不做医学结论。**

### 12.1 分层总览

| 层 | 内容 | 定位 |
|---|---|---|
| T0 已完成 | EAR/眨眼、头部姿态、简易表情、视线信号、CSV 链路 | 原型已跑通 |
| T1 主线新增 | 脉搏波波形（rPPG）、HR/RR、IBI 心跳间期序列（A）；疲劳/专注/压力初级判决（B） | 普通摄像头可行，P0/P1 |
| T2 演示级指数 | 性别、皮肤年龄、肤色变化序列（A，预训练模型/时序采集）；视觉疲劳/情绪稳定性/心理压力综合/心肺适应性/循环应激/生理恢复/自律神经平衡（B，信号加权合成） | 成本低，界面必须标注"日常参考" |
| T3 医学高危 ⚠️ | 血压、血氧、血管弹性、心律不齐、房颤 | 研究级精度，只做"节律异常提示 + 建议就医"话术，禁止诊断性结论；可砍 |
| T4 硬件依赖 | 面部温度（热成像相机）、瞳孔直径（NIR 相机） | 无设备不做，待硬件选型 |

### 12.2 A 模块算法清单

| 优先级 | 算法 | 产出 | 备注 |
|---|---|---|---|
| P0 | 脉搏波波形 | 前额/面颊 ROI 色值信号，POS/CHROM 去伪影，带通 0.7–4Hz | 全部心血管类算法的地基，最难项，关键路径 |
| P0 | HR / RR | 由脉搏波峰值/频域计算 | 协议新增字段（§6 提案 1） |
| P1 | IBI 心跳间期序列 | 相邻搏动峰间隔 | 供 B 做 HRV/自律神经/心律不齐提示 |
| P2 | 性别检测、皮肤年龄预测 | 预训练轻量模型接入 | 用途：个性化称呼/问候 |
| P2 | 肤色变化序列 | 面颊 ROI 色值时序 | 供 B 的代谢应激指数 |
| P2 | 多人脸锁定最大脸 | `max_num_faces` 放开至 2–3，按人脸框面积取主脸 | ✅ 已实现（2026-09-24，config `max_faces`，默认 3） |
| P2 | 口部活动检测 | 唇关键点 13/14 开口度时序 | ✅ A 侧时序已实现（波形 CSV `mouth_open` 列）；协议字段已入 api_doc §3.5 V1.2 草案，待全员确认 |
| T4 | 热成像温度、NIR 瞳孔直径 | — | 设备到位后另行评估 |

### 12.3 B 模块指数合成清单

疲劳度、专注度、精神压力（初级）、视觉疲劳指数、情绪稳定性、心理压力综合、心肺适应性、循环系统应激、生理恢复水平、自律神经平衡——全部为 T0/T1 信号的加权合成，输出 0-100 指数，界面/语音必须注明"日常参考，非医疗结论"。

### 12.4 协议影响

新增信号过 A→B 链路：`hr/rr` 走 §6 提案 1 的字段扩展；脉搏波与 IBI 序列不塞每帧 JSON，按秒聚合打包（具体格式走全员确认后更新 api_doc.md）。

### 12.5 全量算法对照表（27 项，2026-09-23 补充）

> 对照团队算法清单与行业七要素框架的全量盘点：分层沿用 §12.1，"未做"项均已排入 P2 或明确砍掉，无遗漏、无私自扩项。

| # | 算法 | 分层 | 归属 | 状态 / 说明 |
|---|---|---|---|---|
| 1 | 性别检测 | T2 | A | ✅ 已实现（gender_age ONNX + onnxruntime CPU ~3ms，模型缺失自动禁用；lena.jpg 实测校准通道序） |
| 2 | 皮肤年龄预测 | T2 | A | ✅ 随 #1 同模型输出年龄（预测口径 MAE ±5–7 岁，须标"预测"；未做独立皮肤纹理模型） |
| 3 | 面部表情识别 | T0 | A | ✅ 已实现（4 标签简化版，非 7 类基本表情） |
| 4 | 头部姿态估计 | T0 | A | ✅ 已实现（基线相对角度） |
| 5 | 瞳孔直径 | T4 | — | 需 NIR 相机，无设备不做 |
| 6 | 眼睑闭合度/眨眼频率 | T0 | A | ✅ 已实现（EAR+眨眼率）；PERCLOS 由 B 从 EAR 序列计算 |
| 7 | 疲劳度检测 | T1 | B | 融合 EAR+眨眼频率+闭眼时长 |
| 8 | 分心检测 | T1 | B | yaw+视线融合判决 |
| 9 | 专注度指数 | T1 | B | focus 判决（§4 已定义，静默模式用） |
| 10 | 心率 HR | T1 | A | ✅ 已实现（CHROM 色度法，参考级；2026-09-23 由裸绿通道升级） |
| 11 | 呼吸率 RR | T1 | A | ✅ 已实现（20s 窗，参考级） |
| 12 | 血压预测 | T3 ⚠️ | — | 红线：rPPG 文献 MAE 8–15 mmHg，临床不可用；不做 |
| 13 | 血氧预测 | T3 ⚠️ | — | 红线：RGB 精度 ±3–4%，不达医用；不做 |
| 14 | 脉搏波波形+SQI | T1 | A | ✅ 已实现（CHROM 波形 + SQI 门控：SNR<1.5 置灰 HR，间隔 CV>0.3 置灰 IBI） |
| 15 | 血管弹性评估 | T3 ⚠️ | — | 需高精度波形特征（AI/RI/PTT），参考级不可靠 |
| 16 | 心律不齐 | T3 ⚠️ | B | 仅 IBI 序列"节律异常提示"，不诊断 |
| 17 | 房颤筛查 | T3 ⚠️ | — | 仅"不规则节律提示+建议就医"话术 |
| 18 | 循环系统应激指数 | T2 | B | 波形特征加权合成 |
| 19 | 生理恢复水平 | T2 | B | 静息心率恢复速度（需交互配合测量） |
| 20 | 心肺适应性指数 | T2 | B | 心率储备+恢复率合成 |
| 21 | 精神压力评估 | T1/T2 | B | 初级：眨眼+表情+HRV 融合，标注"生理 proxy" |
| 22 | 心理压力综合指数 | T2 | B | 多源 z-score 加权 |
| 23 | 情绪稳定性指数 | T2 | B | A 标签时序滑动方差 |
| 24 | 自律神经平衡指数 | T2 | B | IBI 序列 RMSSD/SDNN——A 的 IBI 质量升级已完成（CV≤0.3 门控） |
| 25 | 视觉疲劳指数 | T2 | B | 眨眼幅度+闭眼时长特征 |
| 26 | 代谢应激指数 | T2 | A+B | ✅ A 侧就绪：双颊 ROI Lab a*/b* 随帧写入波形 CSV（2026-09-23）；B 侧合成待做 |
| 27 | 面部温度 | T4 | — | 需热成像模组，无设备不做 |

**SQI 质量门控链（全链路统一约定）**：人脸检测失败 → 全部输出置灰；ROI/光照不足 → rPPG 置灰；
SNR（带内/带外功率比）< 1.5 或峰间隔 CV > 0.3 → HR/IBI 置灰。置灰输出统一为 `null`/"--"，禁止输出不可信数值。
